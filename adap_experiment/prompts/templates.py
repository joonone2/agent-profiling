"""
ADAP Pilot Experiment — LLM 프롬프트 템플릿
모든 LLM 호출에 사용되는 시스템/유저 프롬프트 문자열 상수를 관리합니다.
"""

# ──────────────────────────────────────────────────────
# Agent Synthesis: Factor Loading → System Prompt 생성
# ──────────────────────────────────────────────────────
AGENT_SYNTHESIS_PROMPT = """다음은 유저 집단에서 발견된 잠재 행동 Factor의 loading 값입니다.

Factor {factor_id} loadings:
{loadings_text}

이 Factor가 나타내는 성향을 가진 가상의 영화 감상자 페르소나를 만들어주세요.
이 페르소나가 앞으로 영화를 평가할 때 따를 System Prompt를 3~5문장으로 작성하세요.
loading이 큰 항목(장르, 인기도 성향 등)을 근거로 판단 기준을 명시하세요.
System Prompt 본문만 출력하고 다른 설명은 붙이지 마세요."""

# ──────────────────────────────────────────────────────
# Agent Judge: 에이전트가 아이템을 평가
# ──────────────────────────────────────────────────────
AGENT_JUDGE_SINGLE_PROMPT = """다음 영화를 1.0~5.0 사이 점수로 평가하고 이유를 한 문장으로 설명하세요.
반드시 아래 JSON 형식으로만 답하세요: {{"score": <float>, "reason": "<한 문장>"}}

영화: {title}
장르: {genres}"""

AGENT_JUDGE_BATCH_PROMPT = """다음 {n}개 영화 각각을 1.0~5.0 사이 점수로 평가하세요.
반드시 아래 JSON 배열 형식으로만 답하세요 (설명 없이):
[{{"item_id": <id>, "score": <float>}}, ...]

영화 목록:
{items_text}"""

# ──────────────────────────────────────────────────────
# Orchestrator: 에이전트 판단을 가중치로 종합
# ──────────────────────────────────────────────────────
ORCHESTRATOR_RATING_PROMPT = """아래는 한 유저(가중치: {weights_text})에 대해 여러 에이전트가 독립적으로 내린 판단입니다.

{agent_outputs_text}

이 가중치를 참고하여 이 유저의 최종 예상 평점을 1.0~5.0 사이 숫자 하나로만 답하세요."""

ORCHESTRATOR_RATING_WITH_HISTORY_PROMPT = """아래는 한 유저(가중치: {weights_text})에 대해 여러 에이전트가 독립적으로 내린 판단입니다.

{agent_outputs_text}

이 유저의 최근 시청 이력은 다음과 같습니다:
{history_text}

최종 예상 평점은 위 에이전트들의 가중 판단(점수와 가중치)을 기본 근거로 계산하세요.
시청 이력은 유저의 전반적인 평가 성향(후하게/박하게 주는 편인지, 어떤 장르를 실제로 즐겨봤는지)을
가늠하는 보조 참고 자료로만 사용하고, 모든 후보에 획일적으로 같은 보정치를 더하지 마세요.
에이전트 판단 간의 상대적 차이가 최종 점수에도 그대로 드러나도록 소수점 첫째 자리까지 세밀하게 판단하세요.
이 유저의 최종 예상 평점을 1.0~5.0 사이 숫자 하나로만 답하세요."""

ORCHESTRATOR_RANKING_PROMPT = """아래는 한 유저(가중치: {weights_text})에 대해 여러 에이전트가 후보 영화 {n}개 각각에 독립적으로 내린 판단 점수입니다.

{items_text}

이 가중치를 참고하여 위 후보 영화들을 이 유저가 선호할 순서대로 순위를 매겨주세요.
반드시 영화 제목만 순서대로 줄바꿈하여 나열하세요."""

ORCHESTRATOR_RANKING_WITH_HISTORY_PROMPT = """아래는 한 유저(가중치: {weights_text})에 대해 여러 에이전트가 후보 영화 {n}개 각각에 독립적으로 내린 판단 점수입니다.

{items_text}

이 유저의 최근 시청 이력은 다음과 같습니다:
{history_text}

후보 간 순위는 위 에이전트 판단 점수와 가중치를 기본 근거로 매기세요.
시청 이력은 유저의 전반적인 평가 성향(선호 장르, 후하게/박하게 주는 편인지)을 가늠하는
보조 참고 자료로만 사용하고, 모든 후보의 순위를 획일적으로 밀어올리지 마세요.
위 후보 영화들을 이 유저가 선호할 순서대로 순위를 매겨주세요.
반드시 영화 제목만 순서대로 줄바꿈하여 나열하세요."""

# ──────────────────────────────────────────────────────
# Baseline (19-dim → LLM)
# ──────────────────────────────────────────────────────
BASELINE_RATING_PROMPT = """당신은 영화 추천 시스템입니다. 유저의 장르별 평균 평점 및 성향 지표는 다음과 같습니다:
{feature_text}

다음 영화에 대해 이 유저가 1.0~5.0점 중 몇 점을 줄지 예측하고 숫자만 답하세요.
영화: {title} (장르: {genres})"""

BASELINE_RANKING_PROMPT = """당신은 영화 추천 시스템입니다. 유저의 장르별 평균 평점 및 성향 지표는 다음과 같습니다:
{feature_text}

다음 {n}개 후보 영화 중 이 유저가 선호할 순서대로 순위를 매겨주세요.
반드시 영화 제목만 순서대로 줄바꿈하여 나열하세요.

후보:
{candidates_text}"""

BASELINE_WITH_HISTORY_RATING_PROMPT = """당신은 영화 추천 시스템입니다. 유저의 장르별 평균 평점 및 성향 지표는 다음과 같습니다:
{feature_text}

이 유저가 과거에 시청한 영화와 평점은 다음과 같습니다:
{history_text}

다음 영화에 대해 이 유저가 1.0~5.0점 중 몇 점을 줄지 예측하고 숫자만 답하세요.
영화: {title} (장르: {genres})"""

BASELINE_WITH_HISTORY_RANKING_PROMPT = """당신은 영화 추천 시스템입니다. 유저의 장르별 평균 평점 및 성향 지표는 다음과 같습니다:
{feature_text}

이 유저가 과거에 시청한 영화와 평점은 다음과 같습니다:
{history_text}

다음 {n}개 후보 영화 중 이 유저가 선호할 순서대로 순위를 매겨주세요.
반드시 영화 제목만 순서대로 줄바꿈하여 나열하세요.

후보:
{candidates_text}"""

# ──────────────────────────────────────────────────────
# ChatGPT-Direct (이력 → LLM)
# ──────────────────────────────────────────────────────
CHATGPT_DIRECT_RATING_PROMPT = """유저가 과거에 다음 영화들을 시청하고 평점을 남겼습니다:
{history_text}

다음 영화에 대해 이 유저가 1.0~5.0점 중 몇 점을 줄지 예측하고 숫자만 답하세요.
영화: {title} (장르: {genres})"""

CHATGPT_DIRECT_RANKING_PROMPT = """유저가 과거에 다음 영화들을 시청했습니다:
{history_text}

다음 {n}개 후보 영화 중 이 유저가 선호할 순서대로 순위를 매겨주세요.
반드시 영화 제목만 순서대로 줄바꿈하여 나열하세요.

후보:
{candidates_text}"""
